# Project Workspace Standard

Project-specific business definitions stay inside `projects/<project>/`.
Reusable code stays in `src/risk_model_workbench/`.

New workflow artifacts belong under
`projects/<project>/versions/<version_id>/`. Legacy `runs/<run_id>/` directories
are compatibility inputs and should not be the default target for new work.
